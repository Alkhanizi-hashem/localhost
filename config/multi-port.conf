request_timeout 30;
cgi_timeout 5;

server {
    host 127.0.0.1;
    ports 8080 8081;
    server_name localhost;

    route / {
        root www;
        methods GET POST DELETE;
        index index.html;
        directory_listing off;
    }
}
